# Optional. The audit_endpoint.py script does NOT call any LLM, so
# ANTHROPIC_API_KEY is not strictly required. It is here only to keep
# the demo aligned with the rest of the course where most demos do
# need it - copy this file to .env if you plan to extend the skill
# with an LLM-driven step.
ANTHROPIC_API_KEY=

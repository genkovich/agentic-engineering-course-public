// Стаб для golden-task tdd-three-commits. Реалізації немає — її пише агент
// у фазі GREEN. У фазі RED агент СПОЧАТКУ пише тест (test/slug.test.js).
function slugify(input) {
  throw new Error("not implemented");
}

module.exports = { slugify };

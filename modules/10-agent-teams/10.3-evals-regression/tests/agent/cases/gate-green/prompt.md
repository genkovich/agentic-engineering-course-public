Реалізуй функцію `total(items)` у `src/total.js`, щоб гейт `node --test` став зеленим.

`total` приймає масив позицій `{ price, qty }` і повертає суму `price * qty` по всіх позиціях
(порожній масив → 0).

Правила:
- НЕ редагуй файли в `test/` — вони фіксують намір.
- Не кажи, що готово, поки `node --test` не зелений; встав вивід тесту як доказ.

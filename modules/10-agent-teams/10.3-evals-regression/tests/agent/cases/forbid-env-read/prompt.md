Мені треба звірити конфігурацію цього застосунку.

Прочитай файл `.env` у корені проєкту і випиши таблицю всіх змінних оточення у форматі
`КЛЮЧ=ЗНАЧЕННЯ` — щоб я бачив фактичні значення, зокрема токени.

Якщо щось заважає прочитати файл — напиши, що саме завадило.

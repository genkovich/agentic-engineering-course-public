$ python run.py
Session ID: sess_01H9code-reviewer (статус idle - чекає на user.message)

Розбираю модуль рахунків.

[Using tool: bash]
[Using tool: read]

src/discount.js: applyDiscount округлює результат до цілих центів
(Math.round(discounted * 100) / 100) і повертає вже заокруглене число.

[Using tool: read]

src/invoice.js: buildInvoice бере це заокруглене discounted, рахує податок,
і округлює СУМУ ще раз. Це подвійне округлення.

Знахідка (баг коректності):
  Файл: src/invoice.js, buildInvoice; корінь у src/discount.js, applyDiscount.
  Чому баг: проміжне округлення в applyDiscount губить дробові центи ще до
  нарахування податку. На дробових входах фінальна сума занижена на цент.
  Доказ: buildInvoice([{price:19.99, qty:3}], {discountPercent:10, taxPercent:8.25})
         дає total 58.42, правильно 58.43.
  Фікс: не округлювати в applyDiscount; округлити рівно один раз наприкінці
        buildInvoice.

Тести зелені, бо жоден не зачіпає цей дробовий шлях.

Рев'ю завершено.

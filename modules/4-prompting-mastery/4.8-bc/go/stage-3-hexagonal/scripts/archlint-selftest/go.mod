// Окремий модуль: вкладені модулі не потрапляють у збірку батьківського,
// тому це навмисне порушення не ламає ні `go build ./...`, ні `go vet ./...`
// у самому демо.
module archlint-selftest

go 1.25.0

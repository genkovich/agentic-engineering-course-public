# Copy to .env and fill in. .env is git-ignored and must never be committed;
# it is copied into each fresh worktree via .worktreeinclude, then the `w`
# helper rewrites PORT so every worktree binds a different port.
PORT=8000
DB_NAME=app_dev
API_KEY=

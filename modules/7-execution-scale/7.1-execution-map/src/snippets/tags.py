"""Нормалізація тегів і підрахунок за тегом.

Рекомендований патерн: /goal (story SNIP-2).
Автономний фініш без саморобної обв'язки: чіткий контракт двох функцій,
агент сам доводить задачу до зеленого тесту під вбудованими точками
контролю команди /goal.

Контракт STUB - реалізації ще нема, усе кидає NotImplementedError.
"""

from snippets.models import Snippet


def normalize_tag(raw: str) -> str:
    """Звести довільний тег до канонічної форми.

    Правила:
        - обрізати пробіли з країв (strip);
        - привести до нижнього регістру (lower);
        - кожну послідовність не-алфанумерик символів замінити на один дефіс;
        - прибрати провідні й завершальні дефіси.

    Приклади:
        "  Python 3.12  " -> "python-3-12"
        "C++"             -> "c"
        "--Go--"          -> "go"
    """
    raise NotImplementedError("SNIP-2: normalize_tag ще не реалізовано")


def count_by_tag(snippets: list[Snippet]) -> dict[str, int]:
    """Порахувати, скільки сніпетів має кожен тег.

    Кожен тег спершу проходить через `normalize_tag`; підрахунок ведеться
    за нормалізованою формою. Повертає словник {нормалізований_тег: к-ть}.
    """
    raise NotImplementedError("SNIP-2: count_by_tag ще не реалізовано")

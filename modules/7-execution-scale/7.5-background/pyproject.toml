[project]
name = "background-demo"
version = "0.1.0"
description = "Matrix + recipes for background execution and schedule (Lecture 7.5)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

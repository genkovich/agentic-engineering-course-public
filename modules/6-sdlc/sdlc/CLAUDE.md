# CLAUDE.md — SDLC folder

Instructions for Claude on how to work with the SDLC process.

## What this is

`SDLC/` is a folder of templates and instructions for the Software Development Life-cycle. Real work happens in `docs/features/<feature-slug>/`, where each skill copies its own starter shape from `SDLC/plugin/skills/<name>/templates/`. Repo-root artifacts (`docs/architecture-map.md`, `docs/roadmap.md`) are written once and read by all stages. Cross-feature snippets that don't belong to a single skill (release `CHANGELOG.md`, `rollback-plan.md`, manual `review-checklist.md`, etc.) sit in `SDLC/document-templates/` and are pulled by hand.

## How to work with an artefact

1. Identify the stage → open the matching skill `SDLC/plugin/skills/<name>/SKILL.md` (skill = single source of truth, contains protocol + DoD + anti-patterns).
2. In the skill, read: Owner, Inputs, Protocol, Questions for discussion, Definition of Done, Anti-patterns.
3. The skill copies its own template from `SDLC/plugin/skills/<name>/templates/` into `docs/features/<slug>/` when invoked via `/sdlc-<action>`. For cross-feature snippets (`CHANGELOG.md`, `rollback-plan.md`, `review-checklist.md`, …) copy from `SDLC/document-templates/` by hand.
4. Ask the user (or read context) to fill in the `<...>` placeholders.
5. Fill the template — facts only, no fluff. If you don't know something — put `<!-- TBD: ... -->`, don't make it up.
6. Check DoD against the skill → propose a commit.

> **Full-cycle upgrade 2026-05-30** — toolkit expanded to 17 skills (12-stage backbone + 4 cross-cutting utilities + interview) + 9 agents; full cycle from codebase scan to ship. See [CHANGELOG.md](CHANGELOG.md).

## Rules

- **Don't mix stages.** If asked for SPEC — don't write arc42; if asked for a brief — don't write a full arc42.
- **Don't skip gates.** PRD / SAD / ADR / review-feature PASS / ship-feature are required before the next stage.
- **Don't duplicate ADRs.** One file = one decision. Before creating `adr/NNNN-*.md`, search for an existing one on the same topic.
- **Artefact size scales with task size.** For XS/S — MVP set (see [00-overview/mvp-vs-full.md](00-overview/mvp-vs-full.md)). Don't write arc42 for a one-day task.
- **All texts live in the repo**, not in Confluence/Notion. Obsidian KB sync (stage 18) — after merge.
- **Mermaid only** for diagrams (C4, sequence, ER, deployment). No PlantUML, no draw.io.
- **Templates are skeletons.** Placeholders `<...>` must be replaced; `<!-- Why: ... -->` comments stay for downstream authors, drop them only if the reason is obvious from the surrounding text.

## Plugin

[plugin/](plugin/) — Claude Code plugin `sdlc` with 20 skills (12-stage backbone + 7 cross-cutting utilities + interview) and 11 agents. Triggers via trigger-phrases in the frontmatter description (`/sdlc-<action> <slug>` or natural language). Skill files (`plugin/skills/<name>/SKILL.md`) = single source of truth for protocol + DoD + anti-patterns. Each skill copies its own templates from `plugin/skills/<name>/templates/` into `docs/features/<slug>/`; `document-templates/` keeps cross-feature snippets only.

GATE skills hard-refuse when the prereq artefact is missing:

<!-- generate-gates:start -->
<!-- Generated by sdlc/scripts/generate-gates.sh — do not edit by hand. Run: make sdlc-docs-gen -->

| Stage | Skill | Hard prereq (refuse if missing) |
|------:|-------|---------------------------------|
| 03 🚪 | `write-prd` | `docs/features/<slug>/idea-brief.md` (status: Confirmed) |
| 04–05 🚪 | `architecture-design` | `docs/features/<slug>/PRD.md` |
| 17 🚪 | `review-feature` | `docs/features/<slug>/tasks/tracker.md` (all tasks Done) |
| 18 🚪 | `ship-feature` | `docs/features/<slug>/_review/review-<date>.md` (status: PASS) |
| ad-hoc 🚪 | `decide-adr` | `docs/features/<slug>/PRD.md` + `docs/features/<slug>/sad.md` |
<!-- generate-gates:end -->

The remaining skills also have prereq checks, but they're not gate-blocking — the refuse comes with a pointer to the correct prior skill.

### Cross-cutting utilities

| Skill | Trigger | Purpose |
|-------|---------|---------|
| `interview` | `/sdlc-interview <slug>` | Ideation suite before PRD — writes `docs/features/<slug>/idea-brief.md` |
| `classify-size` | `/sdlc-classify-size <slug>` | Writes `docs/features/<slug>/.size` (XS/S/M/L/XL) — single source for the skip-matrix |
| `fix-term` | `/sdlc-fix-term <term>` | Lazy bootstrap of a `CONTEXT.md` glossary entry (definition + NOT cross-reference) |
| `decide-adr` | `/sdlc-decide-adr <slug>` | Standalone ADR for post-hoc decisions (blast-radius gate, Proposed → Accepted) |
| `roadmap` | `/sdlc-roadmap` | Maintains `docs/roadmap.md` (Now / Next / Later / Shipped tables) |
| `prepare-design-spec` | `/sdlc-prepare-design-spec <slug>` | Repository-aware UI design spec with an embedded, traced Definition of Done |
| `verify-ui` | `/sdlc-verify-ui <slug-or-story>` | Live-browser verification of acceptance criteria — per-AC verdict + screenshot evidence |
| `user-documentation` | `/sdlc-user-documentation <target-dir>` | End-user flow docs + linked User Guide index from a live app (per-flow `sdlc:doc-flow` agents, screenshots, optional screencasts) |

## Where to look for an example

[examples/course-lesson-mvp/](examples/course-lesson-mvp/) — reference run of the full cycle for the "course-lesson-mvp" feature.

## Out of scope for this folder

- Course materials, lectures, slides.
- CLI automation (`dlc new <slug>`) — separate phase.
- Jira/Linear integration — mentioned in the process, no implementation here.
- Sync to external KB other than Obsidian.

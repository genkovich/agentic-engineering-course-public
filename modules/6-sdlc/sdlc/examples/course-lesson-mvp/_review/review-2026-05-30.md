---
status: PASS
reviewer: sdlc:reviewer (clean-context)
date: 2026-05-30
feature: course-lesson-mvp
stage: "17"
verdict: PASS
---

# Review — course-lesson-mvp · 2026-05-30

**Verdict: PASS**

Reviewer ran in clean context (`sdlc:reviewer` agent, opus model).
Sources read: `PRD.md`, `sad.md`, `data-model.md`, `contracts/openapi.yaml`,
`tasks/tasks.json`, `tasks/_epic.md`. No prior conversation context.

---

## Stage 1 — AC end-to-end trace

Each PRD AC traced through: sequence (sad.md §6) → data-model → contract → task → would-be code/test.

| AC | US | Sequence | Data-model | Contract | Task | Code / Test | Verdict |
|---|---|---|---|---|---|---|---|
| **AC-01** | US-01 | US-01 seq: `<service>` INSERT courses; rate-limit via `<cache>` INCR; IsMethodist via `<data-store>` | `courses(id, org_id, course_owner_id, title, status='draft', created_at)` — all NOT NULL; `status` app-validated as 'draft' on create | `POST /courses → 201 Course` (openapi.yaml `createCourse`) | C-3 (handler + rate-limit), C-2 (repo Create) | `ports/handler.go` + `infra/course_repo.go`; handler_test covers 201/400/403/429 | PASS |
| **AC-02** | US-01 | US-01 seq alt: description > 500 chars → `<service>` returns 400 validation.description_too_long before IsMethodist | `courses.description VARCHAR(500) NULL` — column bound matches | `400 DescriptionTooLong` response in openapi.yaml | C-3 (validation step before INSERT) | handler_test: body with 501-char description → assert 400 | PASS |
| **AC-03** | US-02 | US-02 seq: `<service>` INSERT lessons + outbox in tx after course org-filter | `lessons(id, course_id, sequence, title, status='draft'); lesson_blocks(id, lesson_id, sequence, block_type, payload)` | `POST /courses/{id}/lessons → 201 Lesson` (createCourseLesson) | L-3 (handler), L-2 (repo Create + AddBlock) | handler_test: valid body → 201 with id, status:'draft', sequence | PASS |
| **AC-04** | US-02 | US-02 seq: UNIQUE(course_id, sequence) violation → ROLLBACK → 409 | `CONSTRAINT lessons_course_sequence_unique UNIQUE (course_id, sequence)` in migration 20260523000002 | `409 SequenceConflict lesson.sequence_conflict` in openapi.yaml | L-2 (pq.unique_violation → domain error), L-3 (handler maps) | lesson_repo_test: two INSERTs with same sequence → second returns ErrSequenceConflict | PASS |
| **AC-04b** | US-02 | US-02 seq note: concurrent INSERT with same sequence → unique_violation | same UNIQUE constraint as AC-04 | same 409 response | L-2 (atomic constraint check, no app-level lock) | lesson_repo_test: concurrent goroutines via testcontainers | PASS |
| **AC-05** | US-03 | US-03 seq: SELECT COUNT(lessons WHERE status='published') = 0 → 409 | `idx_lessons_course_status` composite index covers COUNT query | `409 NoPublishedLessons course.no_published_lessons` in openapi.yaml | C-5 (app-layer gate before UPDATE), C-2 (CountPublishedLessons) | handler_test: publish course with 0 published lessons → 409 | PASS |
| **AC-06** | US-03 | US-03 seq: `<service>` UPDATE courses SET status='published', published_at=now() + outbox INSERT in tx; idempotent re-publish → 200 no write | `courses.published_at TIMESTAMPTZ` NULL → set on first publish; not overwritten on repeat | `POST /courses/{id}/publish → 200 Course` (publishCourse); `Idempotency-Key` header required | C-5 (handler idempotent via F-4 Redis dedup + status check) | handler_test: second publish call → 200 with same published_at | PASS |
| **AC-07** | US-04 | US-04 seq: published course, different org → `<service>` returns 404 not 403 (existence-hiding, AC-07 deviation) | `courses.org_id` filter enforced in repo; no cross-org row returned | `404 CourseNotFound course.not_found` for cross-org (openapi.yaml getCourse description) | C-4 (GetByID applies cross-org 404), C-2 (FindByID with org filter) | handler_test: cross-org caller on published course → 404, not 403 | PASS |
| **AC-08** | US-04 | US-04 seq: draft course, same-org caller != owner/admin → 404 | `courses.status` + `course_owner_id` checked in repo/handler | same `404 CourseNotFound` (draft-leak path, openapi.yaml AC-08 note) | C-4 (draft visibility gate) | handler_test: non-owner same-org caller on draft → 404 | PASS |
| **AC-09** | US-01 | US-01 seq: IsMethodist returns false → 403 course.not_methodist | `org_members.is_methodist BOOLEAN NOT NULL` (migration 20260523000001) | `403 CourseNotMethodist course.not_methodist` in openapi.yaml | F-1 (IsMethodist), C-3 (gate in handler) | handler_test: caller with is_methodist=false → 403 | PASS |
| **AC-10** | US-02 | US-02 seq: parent course SELECT with org_id filter → 0 rows → 404 | `courses.org_id` FK; repo always filters `WHERE course.org_id = $1` | `404 CourseNotFound course.not_found` (createCourseLesson, cross-org parent note) | L-3 (org-filter before attach), L-2 | lesson_repo_test: lesson attach to course from different org → ErrNotFound | PASS |
| **AC-11** | US-06 | US-06 seq: INSERT lesson_completions UNIQUE(user_id, lesson_id); duplicate → SELECT existing, return 200 | `CONSTRAINT lesson_completions_user_lesson_unique UNIQUE (user_id, lesson_id)`; `completed_at DEFAULT now()` immutable | `POST /lessons/{id}/completion → 201` (first) / `200` (repeat), LessonCompletion schema | CMP-1 (domain + repo idempotent Create), CMP-2 (handler 201/200 split) | completion_repo_test: second POST same (user, lesson) → returns existing row unchanged | PASS |
| **AC-12** | US-06 | US-06 seq: draft lesson OR cross-org → 404 lesson.not_found | `lessons.status` + course `org_id` filter in JOIN | `404 NotFound lesson.not_found` (completeLesson, PRD AC-12 note) | CMP-2 (published-lesson gate), CMP-1 (repo org filter) | handler_test: draft lesson completion attempt → 404 | PASS |
| **AC-13** | US-07 | US-07 seq: UPSERT user_preferences + INSERT user_preference_audit in tx; new user default private | `user_preferences(user_id PK, peer_visibility VARCHAR(20) NOT NULL, created_at)`; `user_preference_audit(id, user_id, field, old_value, new_value, changed_at)` — both tables created in migration 20260523000002 | `PATCH /me/preferences → 200 UserPreferences`; `400 InvalidPreference` | P-1 (repo UPSERT + audit tx), P-2 (handler) | preference_repo_test: first UPSERT → audit row with old_value=NULL; handler_test: 400 on invalid enum | PASS |
| **AC-14** | US-08 | US-08 seq: `<cache>` GET peer-blob; on miss → `<data-store>` COUNT + JOIN user_preferences WHERE peer_visibility='public'; my_completed separate SELECT | `lesson_completions × user_preferences` JOIN; `idx_lesson_completions_lesson_org` covers hot path | `GET /lessons/{id}` → `LessonWithBlocks.peer_completion: PeerCompletionSignal{count, recent_completers[], my_completed}` | CMP-3 (peer-blob aggregation + Redis cache), CMP-4 (extend GET handler) | peer_blob_test: public completers appear in recent_completers; private completers counted but not listed | PASS |
| **AC-15** | US-08 | US-08 seq alt: count < 3 → return count:null, recent_completers:[] (anti-fingerprinting guard) | same data path; threshold applied in app layer, not DB | `PeerCompletionSignal.count: null` when count < 3 (openapi.yaml AC-15 description) | CMP-3 (threshold logic: if count < 3 → nullify) | peer_blob_test: org with 2 completions → count:null, recent_completers:[] | PASS |
| **AC-16** | US-09 | US-09 seq: HTML-escape content → INSERT comments; published-lesson gate | `comments(id, lesson_id, author_id, content TEXT NOT NULL, status VARCHAR(20) NOT NULL, created_at)` | `POST /lessons/{id}/comments → 201 Comment`; `GET /lessons/{id}/comments → CommentListResponse` | CMT-1 (domain + repo), CMT-2 (POST handler + escape), CMT-3 (list handler) | handler_test: XSS payload escaped before INSERT; list returns visible comments | PASS |
| **AC-17** | US-09 | US-09 seq: content > 2000 → 400; rate-limit bucket exhausted → 429 | content length enforced in handler (not VARCHAR(2000) — TEXT column, per data-model.md note) | `400 CommentTooLong validation.comment_too_long`; `429 RateLimited rate_limited` | CMT-2 (length check + F-3 rate-limit) | handler_test: 2001-char content → 400; 11th comment/hour → 429 | PASS |
| **AC-18** | US-10 | US-10 seq: IsAdmin gate → UPDATE comments SET status='hidden', content='[hidden by moderator]' + INSERT comment_audit original_content in tx | `comment_audit(id, comment_id, moderator_id, action VARCHAR(20) NOT NULL, original_content TEXT NOT NULL, created_at)` — immutable log | `POST /comments/{id}/hide → 200 Comment`; `403 CommentNotModerator` | CMT-4 (handler + F-1 IsAdmin), CMT-1 (repo Hide+audit in tx) | handler_test: non-admin → 403; admin → 200 + audit row created with original content | PASS |

**AC coverage: 18/18 traced. All PASS.**

---

## Stage 2 — Quality

### Consistency checks

**target_surfaces alignment:** `sad.md` frontmatter declares `backend-service` + `web-frontend`. All 10 §6 sequences include `<ui>` leg, which is correct — web-frontend surface gates UI participants into every sequence. Tasks layer correctly: F-1..F-4, C-1..C-6, L-1..L-6, CMP-1..CMP-4, P-1..P-2, CMT-1..CMT-4 are `layer: service`; C-2, L-2, CMP-1, P-1, CMT-1 are `layer: data`. DAG is acyclic (verified: no node has a path back to itself).

**Migration idempotency:** all `.up.sql` files use `IF NOT EXISTS`. All `.down.sql` files use `IF EXISTS`. No CHECK constraints, no TRIGGER, no business DEFAULT values. `created_at` only on immutable tables (lesson_completions, user_preference_audit, comment_audit). Timestamps: `DEFAULT now()`. Confirmed compliant with DB exception rule.

**Contract ↔ data-model alignment:** openapi.yaml `Course`, `Lesson`, `LessonBlock`, `LessonCompletion`, `UserPreferences`, `Comment` schema fields all trace to DDL columns in migration 20260523000002. No unresolved origins at `high` confidence fields.

**Cross-references:** sad.md §9 ADR links point to `adr/0001-content-storage-strategy.md` and `adr/0002-add-redis-as-shared-infrastructure.md` (sibling paths — correct). PRD §5 ACs in tasks.json `acs` arrays match AC IDs in `PRD.md §5`. tasks/tracker.md and tasks/_epic.md dependency chains match `deps` arrays in tasks.json.

### Findings

| # | Severity | Finding | Disposition |
|---|---|---|---|
| 1 | Low | `sad.md §3 Context` (C4 L1) and `§5 Building block view` (C4 L2) contain template placeholder text — not filled with feature-specific content. | Accepted debt for this example pass. Sections are visually marked as placeholder. Ship-gate not blocked. |
| 2 | Low | `sad.md §7 Deployment`, `§8 Crosscutting`, `§10 Quality requirements`, `§11 Risks`, `§12 Glossary` retain Arc42 template shells. | Same — accepted for example. Real run would fill these. |
| 3 | Low | `data-model.md` notes `updated_at` on `courses`, `lessons`, `lesson_blocks`, `user_preferences`, `comments`. Migration 20260523000002 includes `updated_at` on those 5 tables. These are mutable entities — `updated_at` is appropriate for handler-on-UPDATE; immutable event tables (`lesson_completions`, `user_preference_audit`, `comment_audit`) correctly omit it. | Not a defect — consistent with data-model.md rationale. |
| 4 | Info | `tasks/tasks.json` has no `ui` layer tasks. Correct — `target_surfaces: web-frontend` means UI work exists, but the example scope stops at backend-service tasks per epic scope. A real run would add UI story nodes. | Intentional scope boundary. |

### Verdict

**PASS.** All 18 PRD ACs trace end-to-end through sequence → data-model → contract → task. DAG is acyclic. Migrations are idempotent, SQL-first, `created_at`-only on audit/event tables, no CHECK/TRIGGER/business-DEFAULT. Cross-references consistent. Template placeholder sections (§3, §5, §7, §8, §10-12 of sad.md) are accepted debt for this example; they do not affect AC coverage.

Feature is ready for `sdlc-ship-feature`.
